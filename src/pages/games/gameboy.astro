---
// @ts-nocheck
import GameCardsGames from "../../components/gameCardsGames.astro";
import { pokemonGames } from "../../java/pokemonGames";

const games = pokemonGames.gameboy;
---

<h1>Gameboy Games</h1>

<div class="games">
    { games.map((game) => (
        <GameCardsGames game={game} />
        ))
    }
</div>